# ➡️ Paramètre : Orientation départ à droite

Ce document détaille le paramètre `Orientation départ à droite`, qui contrôle le côté d'affichage du message de départ par rapport à la trace.

---

## 🎯 Rôle du Paramètre

Ce paramètre détermine si le panneau du message de départ doit apparaître sur la droite ou la gauche du tracé, vu depuis la caméra.

-   **Type**: Booléen
-   **Valeur par défaut**: `true` (Droite)

---

## 💡 Utilisation

L'orientation est importante pour la visibilité et l'esthétique :
- **Vrai (Coché)** : Le message s'affiche sur la **droite** de la route.
- **Faux (Décoché)** : Le message s'affiche sur la **gauche**.

Choisir le bon côté permet d'éviter que le message ne masque des éléments importants du paysage ou ne sorte du champ de vision si la caméra est dans un virage.
