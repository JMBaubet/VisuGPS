# 🏁 Paramètre : Afficher l'arrivée

Ce document détaille le paramètre `Afficher l'arrivée`, qui permet d'activer l'ajout automatique d'un message à la fin du circuit.

---

## 🎯 Rôle du Paramètre

Ce paramètre détermine si un événement de type "message" doit être généré automatiquement au dernier point du tracé lors de l'importation.

-   **Type**: Booléen
-   **Valeur par défaut**: `true` (Activé)

---

## 💡 Utilisation

Tout comme pour le départ, ce paramètre automatise la finalisation de la vidéo :
- **Activé** : Un message (défini par `Message arrivée`) marque la fin du parcours.
- **Désactivé** : La vidéo se termine sans message spécifique à l'arrivée.
